/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "zngdovnbkulfejgkkqvp.supabase.co", // agrega aquí tu dominio de Supabase
    ],
  },
};

export default nextConfig;
